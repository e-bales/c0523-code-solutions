const fullName = 'Eli Bales';
const isCool = true;
const totalPets = 0;
console.log('fullName = ' + fullName + ". It's type is " + typeof fullName); // using double quotes
console.log('isCool = ' + isCool + ", and it's type is " + typeof isCool);
console.log(
  'totalPets = ' + totalPets + ", and it's type is " + typeof totalPets
);
const unknown = null;
console.log(`unknown is ${unknown}`); // using backticks here
console.log(`The type of unknown is ${typeof unknown}`);
let never;
console.log('never is ' + never); // using single quotes
console.log('The type of never is ' + typeof never);
